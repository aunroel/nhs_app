import SubURL from "./Classes/SubURL";

export const testServerConnection = false;

export const subURLs = {
  todo: new SubURL("todo", "Todo list"),
  subsubLinks: new SubURL("subsubLinks", "Sub sub links")
};

// console.table(classSubURLs);
