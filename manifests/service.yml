apiVersion: v1
kind: Service
metadata:
    name: "nhsapp"
    labels:
        app: "nhsapp"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "nhsapp"